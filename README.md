# 🛒 Order Service

Сервис для управления заказами на **Symfony 7** с интеграцией через Kafka.
Для работы необходимы: balance-service, infrastructure-service, inventory-service

## 🎯 Что делает сервис

- Создание заказов пользователей
- Проверка наличия товаров через inventory-service (Kafka)
- Проверка баланса через balance-service (Kafka)
- Резервирование товаров и списание средств
- Отправка событий о заказах в Kafka

## 🚀 Быстрый старт

### Автоматическая установка (рекомендуется)

```bash
# Запуск скрипта автоматической настройки
./setup.sh
```

Скрипт автоматически:
- Запустит Docker контейнеры
- Установит Symfony через Docker
- Установит зависимости
- Настроит базу данных PostgreSQL
- Настроит подключение к Kafka и Elasticsearch

## 📋 API Endpoints

### Создание заказа

**POST** `http://localhost:8082/api/orders`

```json
{
    "user_id": 1,
    "items": [
        {
            "product_id": 1,
            "warehouse_id": 1,
            "quantity": 5
        },
        {
            "product_id": 2,
            "warehouse_id": 1,
            "quantity": 3
        }
    ]
}
```

**Ответ:**
```json
{
    "success": true,
    "order_id": 1,
    "user_id": 1,
    "status": "pending",
    "total_amount": "800.00",
    "message": "Order created. Processing through Kafka..."
}
```

**Статусы заказа:**
- `pending` - заказ создан, ожидает обработки
- `processing` - заказ обрабатывается
- `completed` - заказ выполнен
- `failed` - заказ не выполнен (недостаточно товара или средств)

### Health check

**GET** `http://localhost:8082/health`

**Ответ:**
```json
{
    "status": "ok"
}
```

## 🔄 Процесс обработки заказа

1. **Создание заказа** → сохранение в БД со статусом `pending`
2. **Отправка событий в Kafka** → `order-events` (событие о создании)
3. **Проверка товаров** → отправка команд в `inventory-commands`
4. **Проверка баланса** → отправка команд в `balance-commands`
5. **Резервирование** → команды в `inventory-commands` и `balance-commands`
6. **Завершение** → статус `completed`, событие в `order-events`

## 🔗 Интеграция через Kafka

Сервис интегрирован с другими сервисами через Kafka:

- **inventory-commands** - отправка команд для проверки и резервирования товаров
- **balance-commands** - отправка команд для проверки баланса и списания средств
- **order-events** - отправка событий о создании и завершении заказов
